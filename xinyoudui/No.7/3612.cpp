#include <bits/stdc++.h>
using namespace std;
int main() {
    string a, b, c;
    cin >> a >> b >> c;
    string tmp;
    vector<int> re;
    int la = 0;
    for (int i = 0; i <= a.size() - b.size(); i++) {
    }
    return 0;
}