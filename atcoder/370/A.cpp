#include <bits/stdc++.h>
using namespace std;
int main() {
    int l, r;
    scanf("%d%d", &l, &r);
    if (l == r) puts("Invalid");
    else if (l == 1) puts("Yes");
    else puts("No");
    return 0;
}